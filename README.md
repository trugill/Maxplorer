# Maxplorer
A self-contained browser tool for visualizing Java (or any) codebase as an interactive web. Drop in a JSON file to explore class relationships, inheritance, and methods through a zoomable, searchable canvas.
